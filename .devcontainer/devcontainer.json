{
    "name": "lockbox",
    "build": {
        "dockerfile": "Dockerfile",
        "context": "..",
		"args": {
			"VARIANT": "1.94_3.14"
		}
    },
	"customizations": {
		"vscode": {
			"extensions": [
				"rust-lang.rust-analyzer"
			]
		}
	},
	"postCreateCommand": "prek install --install-hooks && rustc --version && python --version",
	"remoteUser": "dev"
}